extends layout
block content
    div.content
        h1 בר סלטי�?
        img(src="static/images/logo.jpg")
        form(action='/login', method='post')
            .row
                .large-4.columns
                    label
                        input(name="username", type='text', placeholder="ש�? משתמש", dir="rtl")
            .row
                .large-4.columns
                    label
                        a(dir="rtl", href="/forget_password") שכחת סיסמה?
                        &nbsp;
                        a(dir="rtl", href="/signup") משתמש חדש?
                        input(name="password", type='text', placeholder="סיסמה", dir="rtl")

            .row.center
                button.button(dir="rtl", type='submit') כניסה
            .row.center
                button.button.alert(dir="rtl") כניסה ע�? Gmail
            .row.center
                button.button(dir="rtl") כניסה ע�? Facebook
